import React from 'react'

export default class VoteFooter extends React.Component {
  render() {
    return <>
      <button onClick={ev => {
      }}>支持</button>
      <button onClick={ev => {
      }}>反对</button>
    </>
  }
}